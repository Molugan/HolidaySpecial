<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_column="2"
    android:layout_row="2">
    <EditText android:id="@+id/edit_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:maxLines="1"
        android:hint="@string/edit_message" />
    <com.whatever.holidayspecial.MovieView
        android:id="@+id/movie_screen"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        custom:name="Interstellar"
        custom:synopsis="@string/default_synopsis"
        custom:imgColor="@android:color/holo_green_dark"/>
        <Button android:id="@+id/set_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Plan" />
    <ListView
        android:id="@+id/movie_list"
        android:layout_width="match_parent"
        android:layout_height="404dp"
        />
</LinearLayout>